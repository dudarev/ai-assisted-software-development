This folder is the working area for notes and drafts.

Notes intended for the website should include the `publish` tag in frontmatter:

```yaml
---
title: "Example"
tags: [publish]
---
```

Notes with a `publish` tag are published to the website by the separate site repository.
