# distilled/

This folder stores **distilled content** (organized + summarized + extracted) created from items in `raw/`.

Typical content:
- concise summaries
- extracted entities / concepts / principles
- links back to the raw item and original source URL

Conventions are defined in `docs/distillation/distillation-pipeline.md`.
